import { useState } from 'react';
export default function DemoPage({subkeys,notify}){const [token,setToken]=useState('');const [prompt,setPrompt]=useState('Say hello in exactly 5 words.');return <div className='page'><div className='page-header'><div className='page-title'>Live demo</div></div><div className='card'><select value={token} onChange={e=>setToken(e.target.value)}><option value=''>— select a subkey —</option>{subkeys.filter(s=>s.status==='active').map(s=><option key={s.id} value={s.token}>{s.name}</option>)}</select><input value={prompt} onChange={e=>setPrompt(e.target.value)}/><button className='btn btn-primary' onClick={()=>notify('Demo action kept in SPA UI conversion')}>Run test call →</button></div></div>}
