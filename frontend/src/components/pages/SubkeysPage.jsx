import { useState } from 'react';import '../styles/SubkeysPage.css';
export default function SubkeysPage({ctx}){const {subkeys,api,loadSubkeys,notify,fmtNum,fmtDate,quotaColor}=ctx;const [name,setName]=useState('');const create=async()=>{if(!name)return notify('Enter a name','error');const sk=await api('/api/subkeys',{method:'POST',body:{name,provider:'openai',monthly_token_limit:50000,expires_in_days:null}});if(sk.error)return notify(sk.error,'error');setName('');notify('Subkey created');loadSubkeys();};return <div className='page active'><div style={{padding:'32px 36px'}}><div className='page-header' style={{display:'flex',justifyContent:'space-between'}}><div><div className='page-title'>Subkeys</div><div className='page-sub'>Scoped API tokens you distribute to employees, clients, or teams</div></div><button className='btn btn-primary' onClick={create}>+ Create subkey</button></div><div className='card'><input value={name} onChange={e=>setName(e.target.value)} placeholder='e.g. Client A — Frontend'/></div><div className='card' style={{padding:0}}><div className='table-wrap'><table><thead><tr><th>Name</th><th>Token</th><th>Provider</th><th>Quota</th><th>Expires</th><th>Status</th></tr></thead><tbody>{subkeys.map(sk=>{const pct=Math.min(100,Math.round((sk.tokens_used/sk.monthly_token_limit)*100));const col=quotaColor(sk.tokens_used,sk.monthly_token_limit);return <tr key={sk.id}><td>{sk.name}</td><td>{sk.token?.slice(0,12)}••••••••{sk.token?.slice(-4)}</td><td>{sk.provider}</td><td><div className='quota-bar'><div className={`quota-fill ${col}`} style={{width:`${pct}%`}}/></div><div className='quota-text'>{fmtNum(sk.tokens_used)} / {fmtNum(sk.monthly_token_limit)}</div></td><td>{fmtDate(sk.expires_at)}</td><td><span className={`badge ${sk.status}`}>{sk.status}</span></td></tr>;})}</tbody></table></div></div></div></div>}
