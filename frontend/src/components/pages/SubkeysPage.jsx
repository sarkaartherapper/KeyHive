import { useState } from 'react';
export default function SubkeysPage({subkeys,api,reload,notify}){const [name,setName]=useState('');const create=async()=>{if(!name.trim())return notify('Enter a name','error');const sk=await api('/api/subkeys',{method:'POST',body:{name,provider:'openai',monthly_token_limit:50000,expires_in_days:null}});if(sk.error)return notify(sk.error,'error');setName('');notify('Subkey created');reload();};return <div className='page'><div className='page-header'><div className='page-title'>Subkeys</div></div><div className='card'><input value={name} onChange={e=>setName(e.target.value)} placeholder='e.g. Client A'/><button className='btn btn-primary' onClick={create}>Create subkey</button></div><div className='card table-wrap'><table><thead><tr><th>Name</th><th>Token</th><th>Status</th></tr></thead><tbody>{subkeys.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.token?.slice(0,12)}••••</td><td>{s.status}</td></tr>)}</tbody></table></div></div>}
